/*entry class test2:

    public field j int;
    function main() returns int:
        var i = 5;
        var j = 0;
        while(i > -1) begin
            i--;
            j = 0;
            if(i == 2)
                continue;
            while(j < 7) begin
                if(j == 5)
                    break;
                print(i);
                print(j);
                print("*");
                j++;
            end
        end
        return 0;
    end
end*/

class A:
    public field b int;
    function c(y:Any) returns int:
        return 3;
    end
end

entry class B:
    function main() returns int:
        var x = new A();
        x.b = 1;
        var k = new Any();
        print(x.c(k));
        return 0;
    end
end


//parameter of methodCall
//newarray bool is wrong
//

